pub fn encoder(respType: u8, content: &[u8]) -> [u8] {
    match respType {
        ":" => {
            //logic for resp integer
        }
        "+" => {
            //logic for resp simple integer
        }
        "$" => {
            //logic for resp bulk string
        }
        "*" => {
            //logic for array
        }
    }
    //return content
}

pub fn encoder(resType: u8, content: &[u8]) ->String {
    match resType {
        b'
    }
}
